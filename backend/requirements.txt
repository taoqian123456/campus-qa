# ---------- Web 框架 ----------
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0

# ---------- 数据库 ----------
sqlalchemy>=2.0
pydantic>=2.6

# ---------- AI 与 RAG ----------
openai>=1.30
langchain>=0.2
langchain-openai>=0.1
faiss-cpu>=1.8
numpy>=1.24
pypdf>=4.0
python-docx>=1.1

# ---------- OCR（扫描版 PDF 兜底，可选；config.OCR_ENABLED=0 时不装也可运行） ----------
pypdfium2>=4.30
rapidocr-onnxruntime>=1.3

# ---------- 认证 ----------
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt==4.0.1  # 必须固定：passlib 与 bcrypt>=4.1 不兼容（"AttributeError: module 'bcrypt' has no attribute '__about__'"）

# ---------- 其他 ----------
jieba>=0.42
